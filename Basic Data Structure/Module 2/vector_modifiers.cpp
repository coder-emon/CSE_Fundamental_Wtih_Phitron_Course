#include <bits/stdc++.h>
using namespace std;
int main()
{
    vector <int> v = {1, 2, 4, 2, 7};
    vector <int> v2;
    v2 = v;

    // v2.push_back(10);
    // v2.push_back(20);
    // v2.pop_back();
    // vector <int> v3= {30, 40, 50, 60};
    // v2.insert(v2.begin()+2, 6);    //insert 6 at 2 index
    // v2.insert(v2.begin()+3, 3, -1);    //insert 3 ta -1 at 3 index
    // v2.insert(v2.begin()+v2.size(), v3.begin()+1, v3.begin()+4);    //insert more that one element from other vector;
    // v2.erase(v2.begin()+3, v2.end()-4);

    // replace(v.begin(), v.end() -1, 2, 100);

    // for(int c:v){
    //     cout << c << " ";
    // }
    // cout << endl;
    // // vector <int> ::iterator it = find(v.begin(), v.end(), 2);
    // auto it = find(v.begin(), v.end(), 2);

    // if(it == v.end()){
    //     cout << "Not Found" << endl;
    // }else{
    //     cout << "Found" << endl;
    // }

    // cout << *it << endl;


    return 0;
}