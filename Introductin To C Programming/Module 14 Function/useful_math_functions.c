#include <stdio.h>
#include <math.h>
int main()
{
    // int ans = ceil(4.5);
    // int ans = floor(4.5);
    // int ans = round(3.4);
    // int ans = sqrt(16);
    // int ans = pow(10, 3);
    int ans = abs(-29);
    printf("%d", ans);
return 0;
}